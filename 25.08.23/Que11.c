#include <stdio.h>

int main() {
    int number = 1;  // Initialize the counter

    // Use a while loop to print numbers from 1 to 10
    while (number <= 10) {
        printf("%d\n", number);
        number++;  // Increment the counter
    }

    return 0;
}
